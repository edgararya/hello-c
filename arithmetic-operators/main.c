#include <stdio.h>

int main() {
    // arithmetic operators: +, -, *, /, %, ++, --

    int x = 10;
    int y = 3;
    int z = 0;

    //z = x + y;
    //z = x * y;
    //z = x - y;
    //z = x / y;
    //z = x % y;
    //z = x++; // post-increment
    //z = ++x; // pre-increment
    //z = --y; // pre-decrement
    //z = y--; // post-decrement

    printf("%d", z);
    return 0;
}